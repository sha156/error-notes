# 报错记录：unknown-error

## 日期：2026-04-10 17:11:14

**错误描述**：
{}

**复现步骤**：
1. 运行命令：`cd D:/Project/py/RAG && .venv/Scripts/python.exe -c "
from langchain_community.retrievers import BM25Retriever
from langchain_core.documents import Document
docs = [Document(page_content='test hello world')]
r = BM25Retriever.from_documents(docs)
print('BM25Retriever works:', r.invoke('test'))
" 2>&1`

**环境**：
Windows 11 / Claude Code / Python 3.14.3

**已尝试的修复**：
- (待补充)

**最终解决方式**：
(待补充)

**学到的教训**（给下次 Claude 用）：
- (待补充)
